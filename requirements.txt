# TensorFlow — install BEFORE running pip install -r requirements.txt:
#   Apple Silicon Mac: pip install tensorflow-macos==2.15.0 tensorflow-metal
#   Intel Mac / Linux: pip install tensorflow==2.15.0
# Do NOT install both — they conflict.
numpy>=1.24.0,<2.0.0
pandas>=2.0.0
scipy>=1.10.0
scikit-learn>=1.3.0
lightgbm>=4.0.0
xgboost>=2.0.0
neurokit2>=0.2.0
antropy>=0.2.0
tqdm>=4.65.0
